# TODO:
## the yolo model is recognizing the clocks pretty good! so it needs to be integrating in the main pipeling and crop the bbox the model is recognizing (extra 10 precent), then pass it to the final cGAN generative model.
## fine tune the cGAN model and save the weights to .pth file for future local use